// Suporte do sensor HW-201 — imprimir 2x (um por roda, são idênticos,
// só a posição de montagem na chapa que muda).
include <../parts/params.scad>
use <../parts/hw201_mount.scad>

hw201_bracket_3d(motor_l_center);
